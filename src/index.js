import React from 'react';
import ReactDOM from 'react-dom/client';
import UseReducerTutorialExample from './useReducerTutorialExample';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <UseReducerTutorialExample />
  </React.StrictMode>
);